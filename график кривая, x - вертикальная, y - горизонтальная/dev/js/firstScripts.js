document.addEventListener('DOMContentLoaded', () => {
    const lost = 10;

    console.log(lost);

    //quickFix alt + q (use js booster (default func to arrow etc))

    function add(){
        
    }

});